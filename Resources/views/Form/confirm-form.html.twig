{% block confirm_form_widget %}
    {% spaceless %}
        {% if warning %}
            <div class="alert alert-danger">
                {{ warning|raw }}
            </div>
        {% endif %}
        <div id="confirm-form">
            {{ form(form) }}
            &nbsp;&nbsp;
            <a href="{{ cancelUrl }}">{{ cancelLinkText }}</a>
        </div>
    {% endspaceless %}
{% endblock %}
